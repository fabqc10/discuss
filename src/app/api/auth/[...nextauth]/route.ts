// This implements API's request handlers inside the APP

// If you ever have some outside web service that needs to talk to your next app
// You can create a route handler like this to build a GET request & a POST request handler

import { GET,POST } from "@/auth";


